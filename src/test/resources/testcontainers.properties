# Global Discovery Configuration for Testcontainers
# This forces the project to find Docker on any Windows PC (Home/Pro/Enterprise)
docker.host=npipe:////./pipe/docker_engine
testcontainers.reuse.enable=true
# Use HttpClient5 as the primary transport for Windows 11 compatibility
transport.type=httpclient5
